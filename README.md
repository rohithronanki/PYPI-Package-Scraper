# PYPI-Package-Scraper
Developed a Python-based solution that takes a Python package name as input, retrieves the latest available version from the Python Package Index (PYPI), downloads it, and extracts its dependencies.
